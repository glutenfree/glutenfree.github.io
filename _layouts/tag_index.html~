---
layout: page
title: Рецепты с ингридиентом
---

<h2 class="post_title">{{page.title}}</h2>
<ul>
  {%for post in site.posts%}
  {%for tag in post.tags%}
  {%if tag == page.tag%}
  <div class="row">  <div class="span4">  <img alt="{{ post.title }}" src="/assets/{{ post.title }}/finish.jpg"> </div> <div class="span3">
<span>{{ post.date | date: "%B %e, %Y" }}</span> &raquo; <a href="{{ BASE_PATH }}{{ post.url }}">{{ post.title }}</a></p> <p> {{ post.summary }} </p> </div> </div>
  <p></p>
  {%endif%}
  {%endfor%}
  {%endfor%}
</ul>
